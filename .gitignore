# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (dev tooling only — never required at runtime)
node_modules/
npm-debug.log*
package-lock.json

# Build/scratch
dist/
.cache/
*.local

# Claude Code local session tooling (worktrees, preview launch config) — never shipped
.claude/
